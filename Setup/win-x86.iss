; Script generated by the Inno Script Studio Wizard.
; SEE THE DOCUMENTATION FOR DETAILS ON CREATING INNO SETUP SCRIPT FILES!

[Setup]
; NOTE: The value of AppId uniquely identifies this application.
; Do not use the same AppId value in installers for other applications.
; (To generate a new GUID, click Tools | Generate GUID inside the IDE.)
AppId={{E768EE6E-6153-4D87-9847-5A7C2AA8E14E}
AppName=Warcraft 3 Lua Package Manager
AppVersion=0.5-beta
AppPublisher=Indaxia
AppPublisherURL=https://github.com/Indaxia/WLPM
AppSupportURL=https://github.com/Indaxia/WLPM/issues
DefaultDirName={pf}\WLPM
DefaultGroupName=WLPM x86
DisableProgramGroupPage=yes
LicenseFile=..\LICENSE
OutputBaseFilename=Install WLPM for Windows
Compression=lzma
SolidCompression=yes
ChangesEnvironment=yes
RestartIfNeededByRun=False
ShowLanguageDialog=no
AppReadmeFile=..\README.md
InfoAfterFile=..\README.md
UninstallDisplayName=Warcraft 3 Lua Package Manager (WLPM) x86
AppCopyright=ScorpioT1000 © 2019
VersionInfoProductVersion=0.5
SetupIconFile=..\Resources\install.ico
AlwaysShowGroupOnReadyPage=True
AlwaysShowDirOnReadyPage=True
UninstallDisplayIcon={uninstallexe}
DisableWelcomePage=False

[Files]
Source: "..\bin\Release\netcoreapp2.2\win-x86\publish\*.dll"; DestDir: "{app}" 
Source: "..\bin\Release\netcoreapp2.2\win-x86\publish\*.json"; DestDir: "{app}"   
Source: "..\bin\Release\netcoreapp2.2\win-x86\publish\*.pdb"; DestDir: "{app}" 
Source: "..\bin\Release\netcoreapp2.2\win-x86\publish\wlpm.exe"; DestDir: "{app}"

[Icons]
Name: "{group}\{cm:UninstallProgram,WLPM}"; Filename: "{uninstallexe}"
Name: "{group}\Documentation"; Filename: "https://github.com/Indaxia/WLPM/blob/master/README.md"
Name: "{group}\WLPM"; Filename: "{app}\wlpm.exe"; IconFilename: "{uninstallexe}"; Parameters: "--noexit"

[Registry]
Root: HKLM; \
  Subkey: "SYSTEM\CurrentControlSet\Control\Session Manager\Environment"; \
  ValueType: expandsz; \
  ValueName: "Path"; \
  ValueData: "{olddata};{app}"; \
  Check: NeedsAddPath('{app}') 

[Code]
function NeedsAddPath(Param: string): boolean;
var
  OrigPath: string;
begin
  if not RegQueryStringValue(HKEY_LOCAL_MACHINE,
    'SYSTEM\CurrentControlSet\Control\Session Manager\Environment',
    'Path', OrigPath)
  then begin
    Result := True;
    exit;
  end;
  { look for the path with leading and trailing semicolon }
  { Pos() returns 0 if not found }
  Result := Pos(';' + Param + ';', ';' + OrigPath + ';') = 0;
end;

const
  EnvironmentKey = 'SYSTEM\CurrentControlSet\Control\Session Manager\Environment';

procedure RemovePath(Path: string);
var
  Paths: string;
  P: Integer;
begin
  if not RegQueryStringValue(HKEY_LOCAL_MACHINE, EnvironmentKey, 'Path', Paths) then
  begin
    Log('PATH not found');
  end
    else
  begin
    Log(Format('PATH is [%s]', [Paths]));

    P := Pos(';' + Uppercase(Path) + ';', ';' + Uppercase(Paths) + ';');
    if P = 0 then
    begin
      Log(Format('Path [%s] not found in PATH', [Path]));
    end
      else
    begin
      if P > 1 then P := P - 1;
      Delete(Paths, P, Length(Path) + 1);
      Log(Format('Path [%s] removed from PATH => [%s]', [Path, Paths]));

      if RegWriteStringValue(HKEY_LOCAL_MACHINE, EnvironmentKey, 'Path', Paths) then
      begin
        Log('PATH written');
      end
        else
      begin
        Log('Error writing PATH');
      end;
    end;
  end;
end;

procedure CurUninstallStepChanged(CurUninstallStep: TUninstallStep);
begin
  if CurUninstallStep = usUninstall then
  begin
    RemovePath(ExpandConstant('{app}'));
  end;
end;
